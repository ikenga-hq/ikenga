// Title row slot (WP-20 skeleton, filled by WP-09). Exactly two controls —
// the project chip and the branch chip (spec §6A.4). See ../title-row.tsx.
import { TitleRow } from '@/shell/title-row';

export function TitleRowSlot() {
	return <TitleRow />;
}
